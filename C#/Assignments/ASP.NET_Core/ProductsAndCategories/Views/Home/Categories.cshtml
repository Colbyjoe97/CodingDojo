@model Categories
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="~/css/Categories.css">
</head>
<body>
    @{
        ViewData["Title"] = "Category Page";
    }
    <div class="text-center">
        <h1 class="display-4"><a href="/">Products | </a><a href="/Categories">Categories</a></h1>
    </div><br>
    <div class="card col-4">
        <div class="card-body">
            <div class="card-title text-center">Add New Product</div>
            <form action="addCategory" method="post">
                <span asp-validation-for="Name"></span><br>
                <label asp-for="Name"></label>
                <input asp-for="Name" class="form-control"><br>
                <div class="text-center">
                    <input type="submit" value="Add Category">
                </div>
            </form>
        </div>
    </div>
    <div class="card col-4" id="thisCard">
        <div class="card-body">
            <div class="card-title text-center">Existing Products</div>
            <table class="table table-hover">
                <thead class="text-center">
                    <th>Name</th>
                </thead>
                <tbody class="text-center">
                    @foreach(var p in ViewBag.allCategories)
                    {
                        <tr>
                            <td><a href="category/@p.CategoryId">@p.Name</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>