cd into ruby folder

vagrant up --provision

when window pops up:
click in the box
click capture
randomly hit buttons on keyboard until text goes away

vagrant ssh

cd /vagrant

to leave ssh:
logout

to stop VM:
vagrant halt

to open a text editor inside cmd:
irb

to add new box:
vagrant box add <name>

To delete a VM:
vagrant destroy